server:
  port: 7766

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:./test.db
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
